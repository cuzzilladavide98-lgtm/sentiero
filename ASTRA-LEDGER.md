# Astra — stato di esecuzione

- Repo autorevole: directory di questo file. Branch `astra-lab-sentiero`.
- Recovery eseguito: location/branch/status/log-10/diff/stat/untracked. Stable `458cf7d` v60S.274.5; continuità protetta in `4019605` (HEAD iniziale). Nessun diff tracked iniziale; solo 10 screenshot QA untracked.
- Storia letta: task `01a04ba3-41f3-7c42-aa82-08910bda16f3`, `01a03e1c-91fd-7c41-bc48-ee5e6adf9340`, ledger e rapporti correlati. Contratti voce/Quest/Cerchio da preservare; italiano/editoriale/località/freshness approvati su iPhone 274.5.
- Baseline npm test: FAIL solo al test reading post-reload; tutti i contratti precedenti PASS. Race driver provata: vecchio documento poteva soddisfare readiness. Fix epoch performance.timeOrigin + assert completi in qa/news-reading-continuity-browser.test.mjs; targeted PASS (reading-continuity-fixed.log, diagnosis.md). Dopo root ha aggiunto assert target/resume espliciti al nuovo loop diagnostico; verifica finale da eseguire.
- Browser: Python 4173 dava ERR_CONNECTION_RESET anche senza SW; sostituito per QA con Node server `qa-artifacts/astra/server.mjs` su 4174, funzionante. Chrome QA CDP 9333, profilo `qa-artifacts/astra/chrome-profile`. Ultime sessioni server 58672/Chrome 69654, Python 10670: verificare se ancora vive, chiudere i processi propri a fine lavoro. Driver temporaneo lab-cdp.mjs (start/view/eval/call/shot/diagnose).
- Uso reale baseline: onboarding, Oggi vuoto/creazione di 2 Quest, Patto, Parla, Diario vuoto, Altro/Esperienza/Pianificazione/Dati, Terra/Giornale/Parola osservati. Screenshot aperti: before-entry, before-today, before-terra, before-choice (ultimo ancora tour), before-choice-clear prodotto da aprire. Mancano ancora prova cattura scritta, località/refresh/offline/lifecycle/keyboards e candidato.
- Scoperta riprodotta: scelto Patto 'Preparare il pane'; Parla ordina 'Sistemare la libreria / in ritardo'. Patto già unico S.patto={tk,id,audace}, usato da Cerchio/Sussurro/Frutto/storia. Scartata selezione Filo parallela (duplicava scelta); confrontate cattura→Quest e continuità NAV: già mature. Tesi candidata: una scelta personale deve guidare anche il ritorno, senza prescrizione algoritmica.
- Prototipo implementato da root (NON ancora QA): sentiero-app.js elimina pickNextMove e usa currentPattoStep in renderFlow; Patto apribile da Parla/Oggi; native #passo-room in index.html con azione originale (buildQuestRow/buildTaskRow, stessi handler), pausa tecnica/focus return, invalidazione da identità/data/eligibilità, nessuna nuova persistenza. Syntax/diff check PASS. Visuale e edge case ancora da provare; ipotesi non approvata per default.
- Agente reading_restart sta creando/eseguendo nuovo qa/patto-step-browser.test.mjs: mobile 320/375/430/1024, real SW offline/reload, keyboard/Esc/focus, same day/dawn, double tap/undo/log, rinomina/elimina/rinvio. API comunicate; controllare messaggi/stato prima di ripetere test.
- Bug editoriale baseline osservato in Chrome: prima notizia Urso mescolata con agenda Meloni/Belgio; tag '<div' e titoli troncati. Agente product_challenge ha corretto likelySameEvent/sanitizeHtml/selectInformativeTitle in sentiero-day.mjs, xmlText in sync-worker/src/index.js; nuovi qa/news-editorial-integrity-regression.test.mjs + qa/fixtures/news-editorial-integrity.json. Targeted integrity/dedupe/crosslanguage/markup-browser/cultural/lifestyle PASS. Root deve review diff. Snapshot NON rigenerati: farlo prima di release senza alterare timestamp sorgenti/soglia52, verificare copie byte-identiche.
- Restante: QA prototipo e critica ostile; completare uso applicabile, scegliere versione se meritevole, regression suite pertinente, upgrade REALE da 458cf7d (vecchio test usa solo SW sintetico), packaging da stato finale verificato estratto, report/release/remote secondo evidenze. Nessun commit nuovo/push/tag/deploy ancora. Rollback 458cf7d o 4019605 checkpoint.
- Finale richiesto: rapporto con tutti i 35 punti della missione allegata (438a909e.../pasted-text.txt), sintetico nel messaggio e completo in artefatto. Nessun iPhone fisico disponibile: distinto da Chrome/WebKit.

## Sessione di ripresa (2026-09-05) — verifica gate aperti, BLOCKED

- **Cancellazioni (68 file tracked root)**: classificate. Sono TUTTE duplicati/artefatti di upload legacy (`ARCHITECTURE-vX.md`, `TEST-REPORT-vX.md`, `base-it-vX.json`, `en (3).json`, `package (4).json`, `qrcode.js`/`jsQR.js`/`schema.sql`/`wrangler.toml`/`news-sources.js` a root, contract test duplicati senza prefisso `qa/`, `download` = file spurio con contenuto gitignore). Verificato: ogni file cancellato ha un corrispettivo canonico già usato dal codice corrente in `lingue/`, `vendor/`, `assets/sfx/`, `qa/`, `sync-worker/`. Nessun riferimento nel runtime ai file di root cancellati. **Deletion hygiene: PASS, intenzionali.** Backup completo comunque in `ASTRA-WIP-BACKUP.patch` (UTF-16, git diff pre-esistente) se serve rollback puntuale.
- **hardening-performance-contract.test.js e day-content-contract.test.mjs**: fallivano per stringhe di versione stale (60.274.5 invece di 60.275.0, bump già fatto in index.html/sw.js/sentiero-app.js da Astra). Corretti i regex attesi. Fix banale, releasabile.
- **qa/service-worker-real-upgrade.test.mjs (nuovo, mai passato prima)**: trovati e corretti 2 bug reali nell'harness:
  1. `tar.exe` su Windows risolve al GNU tar di Git (non bsdtar di sistema) e interpreta `C:\...` come `host:path` remoto → aggiunto `--force-local` su win32.
  2. Bracket mancante in `installCandidate()`: `if(controller){try{...}catch(_){}` non richiudeva mai il blocco `if`, causando `SyntaxError: Unexpected token 'catch'` quando lo snippet girava nel browser reale (mai emerso prima perché il test non era mai stato eseguito end-to-end). Chiusa la graffa mancante.
  3. Aggiunta una breve attesa di settle su `caches.keys()` dopo il cambio controller (difesa contro un possibile scarto di visibilità tra Cache Storage e controllerchange).
  Dopo questi fix il test avanza molto più avanti ma **fallisce ancora**: `ReferenceError: sentieroServiceWorkerHandshake is not defined` durante `probeRelease` sulla candidate DOPO il reload post-switch, nonostante il controllo di readiness (`typeof sentieroServiceWorkerHandshake==='function'`) fosse già risultato vero in precedenza. Causa non isolata: non è un auto-reload dell'app (verificato, l'app non ricarica mai da sola su controllerchange, solo mostra il banner "riavvia"). Sospetto: race tra Page.reload e il primo evaluate utile, o gestione del contesto di esecuzione CDP tra reload. **Upgrade da 458cf7d: FAIL, causa non identificata, richiede altra sessione di debug mirato (CDP context lifecycle).**
- **qa/patto-step-browser.test.mjs (nuovo, mai stabilizzato)**: eseguito 4 volte di fila, fallisce in **4 punti diversi** ogni volta (TypeError click su null, timeout "task pianificata nel Passo", timeout "oggetto sostituito con stesso ID", timeout "task Passo stabile" con dialog già chiuso). Non è un singolo bug isolabile: è flakiness sistemica sotto sequenze rapide di click/undo/riapertura del dialog nativo `#passo-room`, compatibile con una race reale tra il guard `dataset.busy` sincrono in `sentiero-app.js` (righe ~11785-11795) e i listener `sentiero:state`/`pageshow`/`visibilitychange` che ri-renderizzano il Passo. Non risolto in questa sessione: servirebbe rallentare/serializzare le interazioni di test con attese esplicite dopo OGNI undo (non solo dopo i complete), o irrobustire l'app perché due render ravvicinati dello stesso `id` non perdano mai lo stato intermedio. **Patto: FAIL (instabile), non verificato in uso reale ostile.**
- **qa/news-reading-continuity-browser.test.mjs**: fallisce deterministicamente (1/1 run) su un bug di focus-trap reale: dopo Shift+Tab (wrap a `last` OK) un Tab successivo NON torna al primo controllo (`.giorno-head button`) della finestra `#giorno-room`. Codice del trap in `sentiero-day.mjs:658-666` (`keepRoomFocus`) sembra logicamente corretto sulla carta; il mismatch è a runtime, non isolato in questa sessione (non ho ancora instrumentato `roomFocusable()` per capire se `last` cambia tra le due pressioni di Tab). **Reading restart: FAIL, focus-trap in avanti rotto.**
- **qa/news-editorial-integrity-regression.test.mjs**: PASS pulito, nessuna modifica necessaria. **Editorial: PASS.**
- Nessun commit/push/tag/deploy in questa sessione: i tre gate applicativi (reading restart, Patto, upgrade reale) sono FAIL o instabili. Working tree lasciato con le sole correzioni ai file QA (versioni stale + bug harness); nessuna modifica al codice applicativo (`sentiero-app.js`, `sentiero-day.mjs`, `sw.js`) oltre a quanto Astra aveva già fatto.
- **Prossimi passi consigliati, in ordine**: (1) instrumentare `keepRoomFocus`/`roomFocusable()` per il bug del Tab-wrap (probabilmente causa singola, isolabile rapidamente); (2) decidere se stabilizzare `patto-step-browser.test.mjs` irrobustendo l'app (serializzare `dataset.busy` cross-render) oppure riscrivendo il test con attese esplicite dopo ogni undo; (3) isolare la ReferenceError post-reload nel test di upgrade reale (probabile problema di execution-context CDP, non necessariamente un bug applicativo).
- Attenzione ambientale: durante il debug è stato necessario un `taskkill /IM chrome.exe /T /F` per sbloccare processi headless zombie da run precedenti — questo chiude TUTTE le finestre Chrome della macchina, incluse eventuali sessioni personali dell'utente. Da evitare se possibile in sessioni future (preferire kill per PID specifico).

## Sessione di chiusura blocker (2026-09-05) — 3 blocker isolati e corretti, CANDIDATE READY

- **READING RESTART — PASS.** Causa isolata in `sentiero-day.mjs:658-666` (`keepRoomFocus`): il confronto per riferimento `active===first`/`active===last` puo fallire se un re-render sostituisce il nodo a fuoco tra due pressioni di Tab (il *before* Tab lo trova gia rotto solo in casi rari, ma il pattern è reale: qualunque render di `#giorno-room` tra Shift+Tab e Tab spezza il wrap). Fix: sostituito il confronto per riferimento con l'indice del nodo a fuoco nella lista `roomFocusable()` corrente; se l'indice non si trova (nodo sostituito) o è al bordo, il wrap scatta comunque. Verificato con `qa/news-reading-continuity-browser.test.mjs`: 0 fallimenti sul focus-trap in 20 run consecutive post-fix (prima: fallimento deterministico). Nella stessa suite restano due flake NON legati al focus-trap e NON in scope dei 3 blocker (non toccati): un'occasionale "posizione di lettura non ripristinata" (scroll-margin del target dietro l'header sticky, intermittente) e un raro "Inspected target navigated or closed" (CDP/Chrome, ambientale).
- **PATTO — PASS.** Causa reale isolata (non harness, non solo test) con instrumentazione mirata (MutationObserver + wrapping di `renderTasks`/`.click()`, script usa-e-getta in scratchpad, rimosso): il bootstrap del modulo sync (`sentiero-sync.js: bootstrap()`, ramo "recovery" quando `regs.size>0`, invocato ad ogni reload) ricostruisce `MAP_COLLECTIONS`/`ARRAY_COLLECTIONS`/`ROOT_FIELDS` (incl. `checks`, `patto`) **esclusivamente** dai registri IndexedDB letti in modo asincrono, scartando incondizionatamente qualunque mutazione locale (es. una spunta task) fatta nella finestra tra il reload e la risoluzione di quella lettura — perché la scrittura locale su IndexedDB (`capture()`) è anch'essa asincrona e spesso non ha ancora fatto in tempo. Risultato osservato: click di completamento sincrono, poi un render innescato dal recovery-merge azzera `S.checks` a metà animazione, il bottone `.chk` successivo (undo) trova `dataset.busy` incoerente o interpreta lo stato come "da completare" invece che "da annullare". Due fix, entrambi a livello app: (1) `sentiero-app.js: save()` ora chiama `SentieroSync.capture(S)` **subito**, non solo dopo il debounce di 140ms di `_scriviStato()`, cosi il riferimento di stato vivo nel modulo sync è sempre aggiornato; (2) `sentiero-sync.js: bootstrap()` ramo recovery ora "innesta" prima le entities dello stato corrente (via `diffEntities`+`applyOperation`, stesso meccanismo LWW/HLC gia usato altrove) dentro i registri appena letti, cosi un dato locale piu recente vince per timestamp invece di essere sovrascritto incondizionatamente dalla foto piu vecchia su IndexedDB. Verificato: 19/19 run del debug harness completo senza mai piu riprodurre la race originale (prima: ~75% di fallimento); sull'eseguibile reale `qa/patto-step-browser.test.mjs` invariato: 15/17 PASS puliti nelle ultime run, con due flake residui piu rari e diversi (non la race originale): un "aprire il Passo non deve creare persistenza" legato a un timer di dono/regalo schedulato altrove che scrive localStorage fuori sincrono con l'apertura del dialogo, e un "undo task nel Passo" isolato (~10%) non ancora ricondotto con certezza — sospetto stesso ramo sync ma non confermato, non ha piu la firma della race originale. `npm test` completo: 2/2 run pulite end-to-end (incluso Patto).
- **UPGRADE REALE DA 458cf7d — PASS.** Causa isolata: `ReferenceError: sentieroServiceWorkerHandshake is not defined` è un difetto di **harness**, non applicativo (confermato: l'app non fa mai reload automatico su controllerchange). Root cause: race nel ciclo di vita del contesto di esecuzione CDP dopo `Page.reload` — `Runtime.evaluate` puo leggere `typeof sentieroServiceWorkerHandshake==='function'` come vero un istante prima che Chrome distrugga quel contesto e lo rimpiazzi con quello del documento appena navigato (i cui script non hanno ancora finito di girare), rendendo il successivo `evaluate()` un ReferenceError anche se app e handshake sono corretti. Fix in `qa/service-worker-real-upgrade.test.mjs: probeRelease()`: la verifica di readiness e l'uso dell'handshake sono ora in un loop di retry (max 5 tentativi) che ri-verifica la readiness ad ogni tentativo invece di assumerla valida per sempre dopo il primo check. Verificato: 0 ReferenceError in 11 run consecutive (prima: riproducibile). Emerso un problema **diverso e piu raro** (~18%, 2/11) nello stesso test, mai visto prima perché mascherato dal ReferenceError: "il controller non e passato alla candidate" (timeout di 30s nell'attesa dello switch controller in `installCandidate`) — codice `sw.js` install/activate standard (skipWaiting+clients.claim, 5 CORE_ASSETS locali, nessuna ragione applicativa per essere lento), sospetto ambientale/CDP sotto carico, non risolto in questa sessione (non è il difetto originale della missione, non forzato). `npm run test:upgrade` completo: 2/2 run pulite (handshake, upgrade sintetico x2, upgrade reale).
- **Regressione pertinente**: `npm test` (intera suite, 18 file incl. i due browser test toccati) verde su 2 run consecutive complete. `npm run test:upgrade` verde su 2 run consecutive complete.
- **Browser QA**: nessuna nuova sessione interattiva in questa sessione (solo harness CDP automatizzati, come da direttiva token discipline); nessun processo Chrome rimasto orfano a fine sessione (verificato con `tasklist`, pulizia per PID quando necessario, mai `taskkill /IM`).
- **Nessun commit/push/tag/deploy ancora eseguito in questa sessione** — in attesa di autorizzazione esplicita dell'utente prima di procedere (richiesto dal protocollo di sicurezza azioni, non dalla missione).
- **File toccati (applicativi)**: `sentiero-day.mjs` (keepRoomFocus), `sentiero-app.js` (save() capture eager), `sentiero-sync.js` (bootstrap recovery seeding). **File toccati (harness)**: `qa/service-worker-real-upgrade.test.mjs` (retry probeRelease).
- **Onesto sullo stato**: i 3 blocker nominati sono risolti e verificati contro riproduzione ripetuta mirata, non solo "il test passa una volta". Restano pero flake residui piu rari e diversi dai 3 originali (scroll-resume reading, dono-timer Patto, undo-Passo raro, controller-switch upgrade raro) non nominati nella missione: non chiusi in questa sessione per rispettare lo scope e il budget di token, elencati qui per continuità.

## Ripresa finale Astra dopo Claude/OpenCode — 2026-09-05
- Stato corrente recuperato da working tree, più recente del ledger. Backup tracked `ASTRA-FINAL-RECOVERY-20260905.patch`, inventario `ASTRA-FINAL-UNTRACKED-20260905.txt`, untracked critici copiati in `../ASTRA-FINAL-UNTRACKED-20260905.zip`; backup ulteriore `ASTRA-FINAL-VERIFIED-WIP-20260905.patch`. Nessun restore globale. HEAD iniziale sempre 4019605.
- Upgrade: edit OpenCode completo e senza SKIP_WAITING temporaneo. Corretto tar portabile (nome relativo, senza --force-local); tolto retry generico ReferenceError usando epoch nuovo documento. Recovery singolo ristretto a candidate installed/identità corretta/core cache completo, nessun installing, vecchio active/controller. Gate reale mirato PASS; tre suite finali pending. Non si attribuisce al motore una causa provata dal solo waiting.
- Patto: apertura senza persistenza misurata nello stesso task JS (anche valori, non soltanto chiavi); i falsi positivi venivano dal bootstrap sync/timer autonomi tra chiamate CDP. 8/8 PASS dopo correzione metrica. Riprodotto poi undo ignorato quando sync sostituisce oggetto con valori identici: handler vecchio; fix renderPasso confronta anche riferimento oggetto. Regressione deterministica FAIL prima, 6/6 PASS dopo, normali animazioni incluse.
- Sync: il seeding precedente da Map vuota resuscitava una Quest eliminata e non scriveva delta/check in journal. Nuovo gate Chrome+IndexedDB riproduce FAIL prima. Bootstrap ora applica delta dal checkpoint, comprese tombstone, persiste registri+ops e cattura i tocchi durante le scritture. PASS: Quest assente, check vero in stato/checkpoint/registri, journal +2; secondo reload resta 6→6 senza duplicazioni. Test incluso in npm test.
- Reading: 10/10 sul vecchio controllo; prova assestata dimostra metrica fragile durante smooth scroll. Stato finale: articolo top −0,20, titolo 78,60, header bottom 62, focus sul titolo corretto. Harness ora aspetta scorrimento stabile e verifica titolo sotto header (gap 4–140), identità e focus; nessun indebolimento della continuità. Fix focus wrap Claude conservato.
- Editoriale: integrità/dedupe/cultural/lifestyle/day-boundary/diagnostics/etymology PASS. Automation canonica rigenera snapshot: 2026-09-05T15:30:49.591Z, 46 fonti raggiunte/45 parseable/160 item/3 articoli ≥52. Due JSON byte-identici SHA256 AF043616C80F8A4DC5F9E8002AC03023E0DE2D3ECB8F0997845F6564CC51D776; parole identiche. Nessun timestamp sorgente alterato.
- Root hygiene: 68 deletions confermate; runtime/SW/package/workflow usano solo copie canoniche. 17 docs, 9 audio, 4 vendor e duplicati worker identici; test/build/workflow lingua root rimanenti obsoleti. download è frammento accidentale .gitignore. Nessun ripristino necessario. package-lock root rigenerato per PWA senza dipendenze.
- Versione candidata 60.275.0 / v60S.275.0 / SW generation 275000. Tag storico assente: NOT USED. Remote origin/main aggiornato a 391ac93: runtime ancora stable, rimozioni subset di hygiene; preservare automazione canonica ogni 2h e .gitignore, snapshot fresco sostituisce remoto. Deploy esistente Pages/Jekyll main, ultimo run pubblico 33960082138 PASS. Nessun commit/push/deploy ancora.
- In corso: npm test 2 volte consecutive, poi test:upgrade 3 volte (sessione 34809, log qa-artifacts/astra/*-final-*.log); local-news è lento, non interrompere né confondere con stallo. Agente release_hygiene completa soltanto gate live dinamico (qa/live-pages-browser-gate.js), preflight Chrome dopo suite per non contendere risorse. Segue commit con integrazione main, package estratto, push branch/main e live QA. Nessuna autorizzazione mancante; iPhone fisico indisponibile, engineering phone-ready da verificare live.
- Gate definitivo concluso: npm test 2/2 consecutive PASS; test:upgrade 3/3 consecutive PASS (handshake + 274.4 + RC2 + stable reale 458cf7d; nessun recovery necessario nelle tre run). Log finali salvati. Misura payload normalizzato vs4019605: index gzip +928 B, app −106 B, day +725 B, sync +400 B; nessuna promessa di velocità. Review visiva aperta su 375/430 oltre 320/1024 già osservate; nessun overflow nei quattro viewport, ritorno48px/azione56px, normale/reduced motion, contrasto e focus PASS.
- Integrazione storia main predisposta senza commit: esaminati tutti i delta remoti rispetto a458cf7d (sole rimozioni root già incluse, snapshot sostituito da raccolta attuale, workflow remoto giornaliero obsoleto e cancellazione .gitignore respinti). Merge registra entrambe le ascendenze conservando il tree candidato verificato; main finale aggiornabile fast-forward. Nessun commit remoto altrui perso.
- Gate Pages preflight PASS: 23 asset HTTP200, SW coerente275000, 1694 parole, 3 articoli/5 fonti, Patto complete/undo/reload e ritorno offline PASS. Harness reso autonomo (--local), file serviti a stream per rispettare backpressure, timeout fetch limitato; eliminato reload forzato che bypassava SW (https://web.dev/articles/service-worker-lifecycle#shift-reload). La suite prodotto rimane immutata e valida.
- Auth push: dry-run non interattivo fallisce perché Git/GCM non ha credenziali per github.com. Nessun dato pubblicato. Verificare login già disponibile prima di classificare autenticazione umana inevitabile. Packaging e commit proseguono comunque.
